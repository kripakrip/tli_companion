# TLI Companion — Дизайн оверлея

> Версия: 1.0
> Дата: 2026-01-13

## 📐 Концепция

### Основные принципы
1. **Минимальное вмешательство** — не мешать игровому процессу
2. **Полупрозрачность** — видеть игру сквозь оверлей
3. **Компактность** — занимать минимум места на экране
4. **Быстрый доступ** — все действия в 1-2 клика
5. **Контекстная информация** — показывать то, что нужно сейчас

---

## 🎨 Структура UI

### Режимы отображения

#### 1. Collapsed (свёрнутый) — минимальный
```
┌──┐
│📊│  ← только кнопка раскрытия + мини-статистика
└──┘
```

#### 2. Compact (компактный) — основной
```
┌──┐ ╭────────────────────────────╮
│📊│ │  02:15:33    12,450 FE     │
│⏸️│ │  ═══════════════════════   │
│💰│ │  В час: 5,533 FE           │
│💸│ ╰────────────────────────────╯
│➕│
│⚙️│
└──┘
```

#### 3. Expanded (расширенный) — полная информация
```
┌──┐ ╭────────────────────────────────────────╮
│📊│ │  ⏱️ 02:15:33        🗺️ 12 карт          │
│⏸️│ │  ════════════════════════════════════  │
│💰│ │                                         │
│💸│ │  💰 Доход:         +12,450 FE          │
│➕│ │  💸 Траты:          -1,200 FE          │
│⚙️│ │  📦 Комиссия:       -1,406 FE          │
└──┘ │  ─────────────────────────────────     │
     │  ✨ Чистая прибыль:  +9,244 FE         │
     │  📈 В час:           +4,108 FE/h       │
     │  ════════════════════════════════════  │
     │  📋 Последний дроп:                    │
     │   • Flame Sand x5         250 FE       │
     │   • Deep Focus          1,200 FE       │
     │   • Aeterna Compass ⚠️    120 FE       │
     ╰────────────────────────────────────────╯
```

---

## 🔘 Вертикальная панель кнопок

| Иконка | Действие | Описание |
|--------|----------|----------|
| 📊 | Toggle View | Переключение compact/expanded |
| ⏸️/▶️ | Pause/Resume | Пауза сессии |
| 💰 | Drops | Открыть полный список дропа |
| 💸 | Expenses | Открыть панель трат |
| ➕ | Add | Быстрое меню: Add Drop / Add Expense |
| ⚙️ | Settings | Настройки оверлея |

---

## 📊 Информационная панель

### Compact Mode (основной)
```
╭──────────────────────────────────╮
│  ⏱️ 02:15:33      💰 12,450 FE   │
│  ▓▓▓▓▓▓▓▓▓▓▓░░░░  📈 5,533/h    │
╰──────────────────────────────────╯
```
- Время сессии
- Общий доход (gross)
- Прогресс-бар (визуально)
- Доход в час

### При наличии трат (показываем чистую прибыль):
```
╭──────────────────────────────────╮
│  ⏱️ 02:15:33      ✨ 10,913 FE   │
│  ▓▓▓▓▓▓▓▓▓▓▓░░░░  📈 4,850/h    │
│  💸 -1,537 FE расходы            │
╰──────────────────────────────────╯
```

---

## 💸 Панель трат (Expenses Panel)

### Быстрое добавление траты
```
╭──────────────────────────────────╮
│  💸 Добавить трату               │
│  ─────────────────────────────── │
│  🧭 Компасы                      │
│  ┌─────────────────────────────┐ │
│  │ Star Net Compass      [10] │ │
│  │ @ 120 FE each = 1,200 FE   │ │
│  └─────────────────────────────┘ │
│                                  │
│  💰 Быстрый ввод:                │
│  [____1500____] FE  [Добавить]   │
│                                  │
│  📋 Траты сессии:                │
│   • Компасы:      -1,200 FE      │
│   • Карты:          -600 FE      │
│   • Другое:           -0 FE      │
│  ─────────────────────────────── │
│  Всего:           -1,800 FE      │
╰──────────────────────────────────╯
```

### Категории трат
1. 🧭 **Компасы** — с выбором типа и количества
2. 🗺️ **Карты/билеты** — входы в контент
3. 💊 **Расходники** — зелья, баффы
4. 🔧 **Крафт** — попытки крафта
5. 💰 **Другое** — произвольная сумма

---

## ➕ Панель добавления дропа

```
╭──────────────────────────────────╮
│  ➕ Добавить дроп                 │
│  ─────────────────────────────── │
│  🔍 [Flame Sa_______________]    │
│     ┌───────────────────────┐    │
│     │ Flame Sand       250  │    │
│     │ Flame Elementium   1  │    │
│     └───────────────────────┘    │
│                                  │
│  Кол-во: [___5___]               │
│  Цена:   250 FE x 5 = 1,250 FE   │
│                                  │
│  [Отмена]        [Добавить]      │
╰──────────────────────────────────╯
```

---

## ⏸️ Режим паузы

При нажатии на паузу:
```
╭──────────────────────────────────╮
│  ⏸️ ПАУЗА                         │
│  ─────────────────────────────── │
│  Сессия: 02:15:33                │
│  (не считается в FE/час)         │
│                                  │
│  [▶️ Продолжить]                  │
╰──────────────────────────────────╯
```

Таймер останавливается, но дроп продолжает записываться.

---

## ⚙️ Настройки оверлея

```
╭──────────────────────────────────╮
│  ⚙️ Настройки                     │
│  ─────────────────────────────── │
│                                  │
│  📐 Размер:  [Compact ▼]         │
│     • Mini / Compact / Full      │
│                                  │
│  🌫️ Прозрачность: [═══●══] 70%   │
│                                  │
│  🎯 Позиция: [Верх-лево ▼]       │
│     • Верх-лево / Верх-право     │
│     • Низ-лево / Низ-право       │
│     • Свободная (drag)           │
│                                  │
│  ⌨️ Горячие клавиши:             │
│     F12 — показать/скрыть        │
│     F11 — пауза                  │
│                                  │
│  📊 Показывать:                  │
│     ☑ Время сессии               │
│     ☑ Доход в час                │
│     ☑ Чистая прибыль             │
│     ☐ Комиссия отдельно          │
│     ☑ Последние дропы            │
│                                  │
│  💱 Комиссия аукциона: [12.5]%   │
│                                  │
╰──────────────────────────────────╯
```

---

## 🎨 Визуальный стиль

### Цветовая палитра
```css
:root {
  /* Основные цвета оверлея */
  --overlay-bg: rgba(15, 15, 20, 0.85);
  --overlay-border: rgba(255, 255, 255, 0.08);
  --overlay-accent: #7c5cff;
  
  /* Текст */
  --text-primary: #ffffff;
  --text-secondary: rgba(255, 255, 255, 0.7);
  --text-muted: rgba(255, 255, 255, 0.4);
  
  /* Статусы */
  --color-income: #4ade80;      /* зелёный — доход */
  --color-expense: #f87171;     /* красный — траты */
  --color-profit: #fbbf24;      /* золотой — чистая прибыль */
  --color-warning: #f97316;     /* оранжевый — предупреждение */
  --color-paused: #60a5fa;      /* синий — пауза */
}
```

### Blur-эффект
```css
.overlay-panel {
  background: var(--overlay-bg);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid var(--overlay-border);
  border-radius: 12px;
  box-shadow: 
    0 4px 24px rgba(0, 0, 0, 0.4),
    inset 0 1px 0 rgba(255, 255, 255, 0.05);
}
```

### Кнопки
```css
.overlay-btn {
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.08);
  border-radius: 8px;
  color: var(--text-secondary);
  cursor: pointer;
  transition: all 0.15s ease;
}

.overlay-btn:hover {
  background: rgba(255, 255, 255, 0.1);
  color: var(--text-primary);
  transform: scale(1.05);
}

.overlay-btn.active {
  background: var(--overlay-accent);
  color: white;
  box-shadow: 0 0 12px rgba(124, 92, 255, 0.4);
}
```

---

## 📱 Адаптивность

### Разрешения экрана
- **1920x1080** — стандартный размер, полный функционал
- **2560x1440** — увеличенные шрифты
- **3840x2160 (4K)** — масштабирование x2

### Позиции по умолчанию
- **Верх-лево** — для левшей или при важном контенте справа
- **Низ-право** — стандартная позиция (рекомендуется)

---

## 🔄 Переходы между режимами

### Анимации
```css
.overlay-panel {
  transition: 
    width 0.2s ease,
    height 0.2s ease,
    opacity 0.15s ease;
}

.overlay-panel.collapsed {
  width: 48px;
  opacity: 0.7;
}

.overlay-panel.collapsed:hover {
  opacity: 1;
}
```

### Состояния
1. **Hidden** → показать (F12 или клик)
2. **Collapsed** ↔ **Compact** (клик на 📊)
3. **Compact** ↔ **Expanded** (клик на 📊 при compact)

---

## 📋 Итоговый расчёт прибыли

### Формула
```
Gross Income    = Σ(drop.quantity × drop.price)
Expenses        = Σ(expense.amount)
Fee             = (Gross Income - Flame Elementium) × 0.125
─────────────────────────────────────────────────────
Net Profit      = Gross Income - Expenses - Fee
Profit per Hour = Net Profit / (duration - paused_time)
```

### Пример отображения
```
╭─────────────────────────────────────╮
│  📊 Итоги сессии (2ч 15м)           │
│  ────────────────────────────────   │
│  💰 Дроп:          +12,450 FE       │
│  💸 Траты:          -1,800 FE       │
│     ├─ Компасы:     -1,200 FE       │
│     ├─ Карты:         -500 FE       │
│     └─ Другое:        -100 FE       │
│  📦 Комиссия 12.5%: -1,331 FE       │
│  ────────────────────────────────   │
│  ✨ Чистая прибыль:  +9,319 FE      │
│  📈 В час:           +4,142 FE/h    │
│  ────────────────────────────────   │
│  🕐 Активное время:  2ч 00м         │
│  ⏸️ На паузе:        15м            │
╰─────────────────────────────────────╯
```

---

## 💾 Сохранение и синхронизация сессий

### Локальное хранение сессий
Каждая сессия сохраняется локально в JSON:

```typescript
interface SavedSession {
  id: string;                    // UUID сессии
  started_at: string;            // ISO timestamp
  ended_at: string | null;       // ISO timestamp или null если активная
  duration_sec: number;          // Общее время
  active_duration_sec: number;   // Время без пауз
  paused_duration_sec: number;   // Время на паузе
  
  // Дроп
  drops: {
    game_id: number;
    quantity: number;
    unit_price: number;
    total_value: number;
    price_source: 'current' | 'previous_season' | 'manual';
  }[];
  
  // Траты
  expenses: {
    category: 'compass' | 'map' | 'consumable' | 'craft' | 'other';
    item_name: string | null;
    amount: number;
    quantity: number;
  }[];
  
  // Итоги
  summary: {
    gross_income: number;        // Сумма дропа
    total_expenses: number;      // Сумма трат
    auction_fee: number;         // Комиссия 12.5%
    net_profit: number;          // Чистая прибыль
    profit_per_hour: number;     // Прибыль в час
    maps_completed: number;      // Карт пройдено
  };
  
  // Синхронизация
  sync_status: 'pending' | 'synced' | 'modified' | 'failed';
  synced_at: string | null;
  modified_at: string | null;    // Когда редактировали
  league_id: string | null;      // ID лиги (SS11, etc.)
  
  // Метаданные
  created_at: string;
  updated_at: string;
  is_deleted: boolean;           // Soft delete для синхронизации
}
```

### Путь хранения
```
Windows: %APPDATA%/com.kripika.tli-companion/sessions/
  ├── active_session.json        # Текущая активная сессия
  ├── 2026-01-13_session_abc123.json
  ├── 2026-01-12_session_def456.json
  └── ...
```

### Синхронизация с kripika.com

#### При завершении сессии:
```
1. Сохранить сессию локально
2. Если онлайн + залогинен:
   → Отправить на сервер
   → Получить подтверждение
   → Пометить sync_status = 'synced'
3. Если оффлайн:
   → Оставить sync_status = 'pending'
   → Синхронизировать позже
```

#### Автоматическая синхронизация:
```
При запуске приложения:
1. Проверить наличие несинхронизированных сессий
2. Если есть pending + онлайн + залогинен:
   → Синхронизировать все pending
3. Показать уведомление: "Синхронизировано X сессий"
```

### API для синхронизации

#### Endpoint: POST /api/tli/sessions
```json
{
  "session_id": "abc-123-def",
  "started_at": "2026-01-13T10:00:00Z",
  "ended_at": "2026-01-13T12:15:00Z",
  "duration_sec": 8100,
  "active_duration_sec": 7200,
  "league_id": "ss11",
  "drops": [...],
  "expenses": [...],
  "summary": {...}
}
```

#### Ответ:
```json
{
  "success": true,
  "session_url": "https://kripika.com/tli/sessions/abc-123-def"
}
```

### UI индикатор синхронизации
```
╭──────────────────────────────────╮
│  ☁️ Синхронизация               │
│  ─────────────────────────────── │
│  ✅ 12 сессий синхронизировано   │
│  ⏳ 2 ожидают синхронизации      │
│                                  │
│  [🔄 Синхронизировать сейчас]    │
╰──────────────────────────────────╯
```

### Редактирование сессий

#### Список сессий (История)
```
╭────────────────────────────────────────────╮
│  📋 История сессий                          │
│  ───────────────────────────────────────── │
│                                             │
│  📅 13 янв 2026                             │
│  ┌─────────────────────────────────────┐   │
│  │ ⏱️ 2ч 15м   ✨ +9,319 FE   ✅ sync  │ ✏️│
│  └─────────────────────────────────────┘   │
│  ┌─────────────────────────────────────┐   │
│  │ ⏱️ 1ч 30м   ✨ +4,500 FE   ⏳ pend  │ ✏️│
│  └─────────────────────────────────────┘   │
│                                             │
│  📅 12 янв 2026                             │
│  ┌─────────────────────────────────────┐   │
│  │ ⏱️ 3ч 00м   ✨ +15,200 FE  ✅ sync  │ ✏️│
│  └─────────────────────────────────────┘   │
│                                             │
╰────────────────────────────────────────────╯
```

#### Редактирование сессии
```
╭────────────────────────────────────────────╮
│  ✏️ Редактировать сессию                    │
│  ───────────────────────────────────────── │
│  📅 13 янв 2026, 10:00 - 12:15              │
│                                             │
│  💰 Дроп:                          [➕ Add] │
│  ┌─────────────────────────────────────┐   │
│  │ Flame Sand        x5    1,250 FE  🗑️│   │
│  │ Deep Focus        x1    1,200 FE  🗑️│   │
│  │ Aeterna Compass   x3      360 FE  🗑️│   │
│  └─────────────────────────────────────┘   │
│                                             │
│  💸 Траты:                         [➕ Add] │
│  ┌─────────────────────────────────────┐   │
│  │ 🧭 Star Net Compass x10  1,200 FE 🗑️│   │
│  │ 🗺️ Map Ticket       x5     500 FE 🗑️│   │
│  └─────────────────────────────────────┘   │
│                                             │
│  📊 Итоги (авто-пересчёт):                  │
│     Доход:     2,810 FE                     │
│     Траты:    -1,700 FE                     │
│     Комиссия:   -351 FE                     │
│     ─────────────────                       │
│     Прибыль:    +759 FE                     │
│                                             │
│  [🗑️ Удалить]              [💾 Сохранить]  │
╰────────────────────────────────────────────╯
```

#### Добавление дропа в сессию
```
╭──────────────────────────────────╮
│  ➕ Добавить дроп                 │
│  ─────────────────────────────── │
│  🔍 [___________________]        │
│                                  │
│  Кол-во: [___1___]               │
│  Цена:   [___auto___] FE         │
│     ☑ Использовать текущую цену  │
│     ☐ Ввести вручную              │
│                                  │
│  [Отмена]        [Добавить]      │
╰──────────────────────────────────╯
```

#### Подтверждение удаления
```
╭──────────────────────────────────╮
│  ⚠️ Удалить сессию?              │
│  ─────────────────────────────── │
│  Сессия: 13 янв, 10:00-12:15     │
│  Прибыль: +9,319 FE              │
│                                  │
│  Это действие необратимо!        │
│                                  │
│  [Отмена]        [🗑️ Удалить]   │
╰──────────────────────────────────╯
```

### Синхронизация изменений

При редактировании уже синхронизированной сессии:
```
1. Локально сохранить изменения
2. Пометить sync_status = 'modified'
3. При следующей синхронизации:
   → PUT /api/tli/sessions/{id}
   → Обновить на сервере
4. sync_status = 'synced'
```

### Права на редактирование
- ✅ Можно редактировать **свои** сессии
- ✅ Можно удалять **свои** сессии
- ❌ Нельзя редактировать чужие (если будет sharing)
- ⚠️ Предупреждение при редактировании старых (>7 дней) сессий

### Восстановление при сбое
Если приложение закрылось во время сессии:
1. При запуске проверить `active_session.json`
2. Если существует — предложить:
   - "Продолжить сессию" (resume)
   - "Завершить и сохранить" (end)
   - "Отменить" (discard)

---

## 🛠️ Технические детали

### Tauri window settings
```rust
// Настройки окна для оверлея
.title("TLI Companion")
.decorations(false)          // Без рамки
.transparent(true)           // Прозрачность
.always_on_top(true)         // Поверх всех окон
.skip_taskbar(true)          // Не показывать в панели задач
.resizable(false)            // Фиксированный размер
```

### Drag & Drop позиция
```typescript
// Сохранение позиции в localStorage
interface OverlayPosition {
  x: number;
  y: number;
  mode: 'collapsed' | 'compact' | 'expanded';
  opacity: number;
}
```

### Глобальные хоткеи
```rust
// Регистрация глобальных горячих клавиш
use tauri::GlobalShortcut;

app.global_shortcut_manager()
   .register("F12", || { toggle_visibility() })
   .register("F11", || { toggle_pause() })
```

---

## 📅 План реализации

### Фаза 3.1: Базовый оверлей
- [ ] Новый layout с вертикальными кнопками
- [ ] Полупрозрачный фон с blur
- [ ] Три режима: collapsed/compact/expanded
- [ ] Сохранение позиции окна

### Фаза 3.2: Функциональность сессии
- [ ] Кнопка паузы (время не считается)
- [ ] Панель добавления дропа вручную
- [ ] Панель добавления трат
- [ ] Расчёт комиссии 12.5%
- [ ] Расчёт чистой прибыли

### Фаза 3.3: Сохранение и синхронизация
- [ ] Локальное сохранение сессий в JSON
- [ ] Автосохранение активной сессии (recovery)
- [ ] Синхронизация с kripika.com при завершении
- [ ] Очередь pending сессий для оффлайн режима
- [ ] Индикатор статуса синхронизации

### Фаза 3.4: История и редактирование
- [ ] Список сессий (история)
- [ ] Просмотр деталей сессии
- [ ] Редактирование: добавление/удаление дропа
- [ ] Редактирование: добавление/удаление трат
- [ ] Удаление сессий (с подтверждением)
- [ ] Синхронизация изменений (PUT на сервер)

### Фаза 3.5: Полировка
- [ ] Горячие клавиши (F12, F11)
- [ ] Анимации переходов
- [ ] Настройки прозрачности
- [ ] Drag & drop позиции
- [ ] История сессий (просмотр прошлых)

---

*Документ создан: 2026-01-13*
